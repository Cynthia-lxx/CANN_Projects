// Generated by CANN_OpHelper verifygen -- aclnn single-op host runner.
// API pattern mirrors the official minimal sample
// (DOC 09_course_practice/src/09.01_testcase/testcase_6/aclnn_test.cpp).
#include <cstdint>
#include <cstdio>
#include <fstream>
#include <numeric>
#include <string>
#include <vector>

#include "acl/acl.h"
#include "aclnn_asc_try.h"

#define SUCCESS 0
#define FAILED 1

#define CHECK_RET(cond, return_expr) \
    do {                             \
        if (!(cond)) {               \
            return_expr;             \
        }                            \
    } while (0)

#define LOG_PRINT(message, ...)         \
    do {                                \
        printf(message, ##__VA_ARGS__); \
    } while (0)

int64_t GetShapeSize(const std::vector<int64_t> &shape)
{
    return std::accumulate(shape.begin(), shape.end(), static_cast<int64_t>(1), std::multiplies<int64_t>());
}

template <typename T>
bool ReadBinFile(const std::string &fileName, std::vector<T> &data)
{
    std::ifstream input(fileName, std::ios::binary);
    if (!input) {
        LOG_PRINT("open %s failed\n", fileName.c_str());
        return false;
    }
    input.read(reinterpret_cast<char *>(data.data()), static_cast<std::streamsize>(data.size() * sizeof(T)));
    if (input.gcount() != static_cast<std::streamsize>(data.size() * sizeof(T))) {
        LOG_PRINT("read %s failed, expected %zu bytes, got %lld bytes\n", fileName.c_str(),
                  data.size() * sizeof(T),
                  static_cast<long long>(input.gcount()));
        return false;
    }
    return true;
}

template <typename T>
bool WriteBinFile(const std::string &fileName, const std::vector<T> &data)
{
    std::ofstream output(fileName, std::ios::binary);
    if (!output) {
        LOG_PRINT("open %s failed\n", fileName.c_str());
        return false;
    }
    output.write(reinterpret_cast<const char *>(data.data()), static_cast<std::streamsize>(data.size() * sizeof(T)));
    return output.good();
}

int Init(int32_t deviceId, aclrtStream *stream)
{
    auto ret = aclInit(nullptr);
    CHECK_RET(ret == ACL_SUCCESS, LOG_PRINT("aclInit failed. ERROR: %d\n", ret); return FAILED);
    ret = aclrtSetDevice(deviceId);
    CHECK_RET(ret == ACL_SUCCESS, LOG_PRINT("aclrtSetDevice failed. ERROR: %d\n", ret); return FAILED);
    ret = aclrtCreateStream(stream);
    CHECK_RET(ret == ACL_SUCCESS, LOG_PRINT("aclrtCreateStream failed. ERROR: %d\n", ret); return FAILED);
    return SUCCESS;
}

template <typename T>
int CreateAclTensor(const std::vector<T> &hostData, const std::vector<int64_t> &shape, void **deviceAddr,
                    aclDataType dataType, aclTensor **tensor)
{
    auto size = GetShapeSize(shape) * sizeof(T);
    auto ret = aclrtMalloc(deviceAddr, size, ACL_MEM_MALLOC_HUGE_FIRST);
    CHECK_RET(ret == ACL_SUCCESS, LOG_PRINT("aclrtMalloc failed. ERROR: %d\n", ret); return FAILED);

    ret = aclrtMemcpy(*deviceAddr, size, hostData.data(), size, ACL_MEMCPY_HOST_TO_DEVICE);
    CHECK_RET(ret == ACL_SUCCESS, LOG_PRINT("aclrtMemcpy failed. ERROR: %d\n", ret); return FAILED);

    *tensor = aclCreateTensor(shape.data(), shape.size(), dataType, nullptr, 0, aclFormat::ACL_FORMAT_ND, shape.data(),
                              shape.size(), *deviceAddr);
    CHECK_RET(*tensor != nullptr, LOG_PRINT("aclCreateTensor failed\n"); return FAILED);
    return SUCCESS;
}

void DestroyResources(const std::vector<void *> &tensors, const std::vector<void *> &deviceAddrs, aclrtStream stream,
                      int32_t deviceId, void *workspaceAddr = nullptr)
{
    for (size_t i = 0; i < std::min(tensors.size(), deviceAddrs.size()); i++) {
        if (tensors[i] != nullptr) {
            aclDestroyTensor(reinterpret_cast<aclTensor *>(tensors[i]));
        }
        if (deviceAddrs[i] != nullptr) {
            aclrtFree(deviceAddrs[i]);
        }
    }
    if (workspaceAddr != nullptr) {
        aclrtFree(workspaceAddr);
    }
    aclrtDestroyStream(stream);
    aclrtResetDevice(deviceId);
    aclFinalize();
}

int main(int argc, char **argv)
{
    const std::string dataDir = argc > 1 ? argv[1] : ".";

    int32_t deviceId = 0;
    aclrtStream stream = nullptr;
    auto ret = Init(deviceId, &stream);
    CHECK_RET(ret == SUCCESS, LOG_PRINT("Init acl failed. ERROR: %d\n", ret); return FAILED);

    const int64_t elementCount = 16384;
    const std::vector<int64_t> tensorShape = {16384};

    std::vector<float> outputCHostData(elementCount, 0.0f);
    std::vector<float> inputAHostData(elementCount);
    std::vector<float> inputBHostData(elementCount);
    CHECK_RET(ReadBinFile(dataDir + "/input_A.bin", inputAHostData),
              LOG_PRINT("read input file failed\n"); return FAILED);
    CHECK_RET(ReadBinFile(dataDir + "/input_B.bin", inputBHostData),
              LOG_PRINT("read input file failed\n"); return FAILED);

    void *inputADeviceAddr = nullptr;
    aclTensor *inputA = nullptr;
    void *inputBDeviceAddr = nullptr;
    aclTensor *inputB = nullptr;
    void *outputCDeviceAddr = nullptr;
    aclTensor *outputC = nullptr;
    void *workspaceAddr = nullptr;

    auto cleanup = [&]() {
        DestroyResources({inputA, inputB, outputC}, {inputADeviceAddr, inputBDeviceAddr, outputCDeviceAddr}, stream, deviceId, workspaceAddr);
    };

    ret = CreateAclTensor(inputAHostData, tensorShape, &inputADeviceAddr, ACL_FLOAT, &inputA);
    CHECK_RET(ret == SUCCESS, LOG_PRINT("create input tensor A failed. ERROR: %d\n", ret);
              cleanup(); return FAILED);
    ret = CreateAclTensor(inputBHostData, tensorShape, &inputBDeviceAddr, ACL_FLOAT, &inputB);
    CHECK_RET(ret == SUCCESS, LOG_PRINT("create input tensor B failed. ERROR: %d\n", ret);
              cleanup(); return FAILED);
    ret = CreateAclTensor(outputCHostData, tensorShape, &outputCDeviceAddr, ACL_FLOAT, &outputC);
    CHECK_RET(ret == SUCCESS, LOG_PRINT("create output tensor failed. ERROR: %d\n", ret);
              cleanup(); return FAILED);

    uint64_t workspaceSize = 0;
    aclOpExecutor *executor = nullptr;
    ret = aclnnAscTryGetWorkspaceSize(inputA, inputB, outputC, &workspaceSize, &executor);
    CHECK_RET(ret == ACL_SUCCESS, LOG_PRINT("aclnnAscTryGetWorkspaceSize failed. ERROR: %d\n", ret);
              cleanup(); return FAILED);

    if (workspaceSize > 0) {
        ret = aclrtMalloc(&workspaceAddr, workspaceSize, ACL_MEM_MALLOC_HUGE_FIRST);
        CHECK_RET(ret == ACL_SUCCESS, LOG_PRINT("allocate workspace failed. ERROR: %d\n", ret);
                  cleanup(); return FAILED);
    }

    ret = aclnnAscTry(workspaceAddr, workspaceSize, executor, stream);
    CHECK_RET(ret == ACL_SUCCESS, LOG_PRINT("aclnnAscTry failed. ERROR: %d\n", ret);
              cleanup(); return FAILED);

    ret = aclrtSynchronizeStream(stream);
    CHECK_RET(ret == ACL_SUCCESS, LOG_PRINT("aclrtSynchronizeStream failed. ERROR: %d\n", ret);
              cleanup(); return FAILED);

    std::vector<float> resultData(elementCount);
    ret = aclrtMemcpy(resultData.data(), resultData.size() * sizeof(resultData[0]), outputCDeviceAddr,
                      resultData.size() * sizeof(resultData[0]), ACL_MEMCPY_DEVICE_TO_HOST);
    CHECK_RET(ret == ACL_SUCCESS, LOG_PRINT("copy result from device to host failed. ERROR: %d\n", ret);
              cleanup(); return FAILED);

    CHECK_RET(WriteBinFile(dataDir + "/output.bin", resultData),
              LOG_PRINT("write output.bin failed\n"); cleanup(); return FAILED);

    cleanup();
    LOG_PRINT("write output to %s/output.bin success\n", dataDir.c_str());
    return SUCCESS;
}
